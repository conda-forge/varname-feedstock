{% set name = "varname" %}
{% set version = "0.8.3" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/varname-{{ version }}.tar.gz
  sha256: 53283f002ea6757a6157cc362cd9464243687bf7d88d7d49a9c853f956e73973

build:
  number: 0
  noarch: python
  script:
    - {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - poetry >=0.12
    - python >=3.6
  run:
    - asttokens >=2.0.0,<3.0.0
    - executing >=0.8.3,<0.9.0
    - pure_eval <1.0.0
    - python >=3.6

test:
  imports:
    - varname
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/pwwang/python-varname
  summary: Dark magics about variable names in python.
  license: MIT
  # Check with upstream for license file
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
